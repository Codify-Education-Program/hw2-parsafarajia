//Write a function that takes an index and an array, and returns
// printIndex(2, [5, 10, 15, 20])
function printIndex(index, array) {
    return array[index];
}

// Example usage:
console.log(printIndex(0, ['a', 'b', 'c'])); // Output: 'a'